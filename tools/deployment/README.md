"""Build-time model export / compilation helpers (not used at inference runtime).

Future scripts (stubs only in phase 1):

- export_onnx.py
- build_horizon.py
- build_rknn.py

Runtime must only load already-compiled artifacts.
"""
